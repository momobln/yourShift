#  YOURSHIFT Project Structure Explanation  
(Arabic → English)
## 🇸🇦 الشرح بالعربية

### المجلد الرئيسي (Root Folder)
اسم المشروع: **YOURSHIFT**  
هو المجلد الذي يحتوي على جميع الملفات والإعدادات الخاصة بالمشروع.


###  `.next`
- يُنشأ تلقائيًا بواسطة Next.js عند تشغيل المشروع.
- يحتوي على الملفات **المبنية (Build Output)**.
- لا يُعدل يدويًا.

 **Frontend** (خاصة بتشغيل الواجهة).

###  `app/`
هو قلب تطبيق **Next.js**.  
يحتوي على الصفحات (Frontend) والـ API (Backend).

####  `app/api/guards/route.ts`
- هذا ملف الـ **Backend**.
- يعرّف نقطة الوصول `/api/guards`.
- يستخدم Prisma للتعامل مع قاعدة البيانات (CRUD).

 **Backend**


####  `app/guards/page.tsx`
- صفحة واجهة المستخدم لعرض بيانات الحراس.
- تستورد مكونات مثل `GuardForm`.

 **Frontend**

####  `app/layout.tsx`
- يحتوي على الهيكل العام (Layout) لجميع الصفحات.
- مثل الـ Header أو Footer.

 **Frontend**

---

####  `app/page.tsx`
- الصفحة الرئيسية (`/`).
- يمكن وضع مقدمة المشروع أو روابط الصفحات.

 **Frontend**

####  `app/globals.css`
- تنسيقات عامة (ألوان، خطوط، خلفية).

 **Frontend**

###  `components/`
- يحتوي على مكونات قابلة لإعادة الاستخدام.

#### `GuardForm.tsx`
- مكون خاص بإضافة أو تعديل بيانات الحراس.
- يتصل بـ `/api/guards`.

###  `prisma/`
- يربط المشروع بقاعدة البيانات عبر Prisma ORM.

#### `schema.prisma`
- يعرّف نماذج الجداول والحقول في قاعدة البيانات.

#### `migrations/`
- يسجل تغييرات قاعدة البيانات بعد كل أمر `npx prisma migrate dev`.
 **Backend**

###  `public/`
- يحتوي على ملفات ثابتة (صور، شعارات، أيقونات...).

 **Frontend**

---

### ⚙️ ملفات إعداد مهمة

| الملف | الوظيفة | النوع |
|--------|----------|--------|
| `.env` | متغيرات البيئة (كلمة مرور قاعدة البيانات وغيرها) | Backend |
| `.gitignore` | تجاهل ملفات مثل `node_modules` و `.env` | Dev |
| `docker-compose.yml` | لتشغيل قاعدة البيانات (PostgreSQL) في Docker | Backend |
| `eslint.config.mjs` | تدقيق الكود | Dev |
| `postcss.config.mjs` | إعداد Tailwind أو PostCSS | Frontend |
| `next.config.ts` | إعدادات خاصة بـ Next.js | Frontend |
| `tsconfig.json` | إعدادات TypeScript | Dev |
| `package.json` | الحزم والأوامر (`npm run dev`) | Full project |
| `README.md` | توثيق المشروع | Documentation |



### ⚙️ Configuration Files Summary

| File | Purpose | Type |
|------|----------|------|
| `.env` | Environment variables (DB credentials) | Backend |
| `.gitignore` | Ignore sensitive or generated files | Dev |
| `docker-compose.yml` | Runs database container | Backend |
| `eslint.config.mjs` | Code linting rules | Dev |
| `postcss.config.mjs` | Tailwind or PostCSS setup | Frontend |
| `next.config.ts` | Next.js settings | Frontend |
| `tsconfig.json` | TypeScript configuration | Dev |
| `package.json` | Dependencies and scripts | Full project |
| `README.md` | Documentation | Documentation |


````
| المفهوم             | ملف أو مجلد                 | الغرض               |
| ------------------- | --------------------------- | ------------------- |
| صفحة واجهة المستخدم | `app/guards/page.tsx`       | عرض البيانات        |
| نموذج إدخال         | `components/GuardForm.tsx`  | إدخال بيانات جديدة  |
| منطق API            | `app/api/guards/route.ts`   | تخزين وجلب البيانات |
| قاعدة البيانات      | `prisma/schema.prisma`      | تعريف الجداول       |
| البيئة              | `.env`                      | إعداد الاتصال       |
| Docker              | `docker-compose.yml`        | تشغيل PostgreSQL    |
| التصميم العام       | `layout.tsx`, `globals.css` | مظهر موحد           |
 **UI** تعني **User Interface** أي **واجهة المستخدم**.
هي الجزء الذي يراه ويتفاعل معه المستخدم في المتصفح أو التطبيق.

###  بالعربية

واجهة المستخدم = الشكل الخارجي للموقع أو التطبيق
مثلاً:

* الأزرار (Buttons)
* النماذج (Forms)
* الألوان والخطوط
* ترتيب الصفحات والعناصر

هي التي تبنيها في ملفات مثل:

* `page.tsx` → صفحة تعرض البيانات
* `GuardForm.tsx` → نموذج إدخال
* `globals.css` → تنسيق الألوان والخطوط

 باختصار: **UI = ما يراه المستخدم**

---

###  In English

**UI (User Interface)** means the visible part of your app — what users see and interact with.

Examples:

* Buttons
* Forms
* Colors and fonts
* Page layout and structure

In your project, UI is built using:

* `page.tsx` → Displays content
* `GuardForm.tsx` → Input form
* `globals.css` → Styles

>  In short: **UI = what the user sees and clicks**


| نوع الطلب | الدالة     | الوظيفة           | أمر Prisma    |
| --------- | ---------- | ----------------- | ------------- |
| GET       | `GET()`    | جلب كل الحراس     | `.findMany()` |
| POST      | `POST()`   | إنشاء حارس جديد   | `.create()`   |
| DELETE    | `DELETE()` | حذف حارس بالـ ID  | `.delete()`   |
| PUT       | `PUT()`    | تحديث بيانات حارس | `.update()`   |
| HTTP Method | Function   | Purpose            | Prisma Command |
| ----------- | ---------- | ------------------ | -------------- |
| GET         | `GET()`    | Fetch all guards   | `.findMany()`  |
| POST        | `POST()`   | Add new guard      | `.create()`    |
| DELETE      | `DELETE()` | Delete guard by ID | `.delete()`    |
| PUT         | `PUT()`    | Update guard by ID | `.update()`    |

@prisma/client → الحزمة الرسمية من فريق Prisma

| الجزء                | المعنى                 | الوظيفة                        |
| -------------------- | ---------------------- | ------------------------------ |
| `e`                  | كائن الحدث             | يحتوي تفاصيل ما حدث في النموذج |
| `React.FormEvent`    | نوع الحدث              | يُستخدم مع TypeScript          |
| `e.preventDefault()` | منع إعادة تحميل الصفحة | إبقاء الصفحة كما هي            |
| `async`              | دالة غير متزامنة       | تسمح باستخدام `await` للانتظار |
| Code Part            | Meaning               | Purpose                                 |
| -------------------- | --------------------- | --------------------------------------- |
| `e`                  | event object          | describes what happened                 |
| `React.FormEvent`    | type of the event     | helps TypeScript know it’s a form event |
| `e.preventDefault()` | stop default reload   | keep the page from refreshing           |
| `async`              | asynchronous function | allows `await` for API calls            |


| Code               | Meaning                         | Purpose                |
| ------------------ | ------------------------------- | ---------------------- |
| `JSON.stringify()` | Convert JS object → JSON string | To send data to API    |
| `req.json()`       | Convert JSON string → JS object | To read data on server |
| الكود              | المعنى                   | الوظيفة            |
| ------------------ | ------------------------ | ------------------ |
| `JSON.stringify()` | تحويل الكائن إلى نص JSON | لإرساله إلى الخادم |
| `req.json()`       | تحويل النص إلى كائن      | لقراءته في السيرفر |


| العنصر   | الوظيفة                               |
| -------- | ------------------------------------- |
| `<form>` | لإدخال أو تعديل بيانات الحارس         |
| `input`  | حقل لكتابة الاسم أو الإيميل أو الهاتف |
| `button` | إرسال أو تعديل البيانات               |
| `<ul>`   | لعرض قائمة الحراس                     |
| `Edit`   | لملء النموذج ببيانات حارس لتعديله     |
| `Delete` | لحذف حارس من القائمة                  |

| السطر              | الوظيفة                          |
| ------------------ | -------------------------------- |
| `import GuardForm` | استيراد مكون النموذج والقائمة    |
| `GuardsPage()`     | تعريف صفحة جديدة                 |
| `<main>`           | الحاوية الأساسية                 |
| `<h1>`             | العنوان الرئيسي للصفحة           |
| `<GuardForm />`    | إدراج النموذج داخل الصفحة        |
| `export default`   | تصدير الصفحة لتعمل على `/guards` |
| المفهوم                | الشرح                                          |
| ---------------------- | ---------------------------------------------- |
| `useEffect()`          | لتحميل البيانات عند أول تحميل للصفحة           |
| `fetch("/api/shifts")` | جلب البيانات من الـ API الخاص بنا              |
| `cache: "no-store"`    | حتى لا يحتفظ Next.js بنسخة قديمة من البيانات   |
| `map()`                | تستخدم لعرض كل وردية في صف خاص بها بالجدول     |
| `toLocaleDateString()` | لتحويل التاريخ إلى تنسيق قابل للقراءة للمستخدم |

install ZOD 
| المرحلة | الهدف                                                 |
| ------- | ----------------------------------------------------- |
| 1️⃣     | تجهيز قاعدة البيانات لمستخدمي النظام (User model)     |
| 2️⃣     | إعداد بيئة NextAuth (تنصيب + ملف الإعدادات)           |
| 3️⃣     | إنشاء **صفحة التسجيل (Signup)** مع تشفير كلمة المرور  |
| 4️⃣     | إنشاء **صفحة تسجيل الدخول (Login)** باستخدام NextAuth |
| 5️⃣     | ربط الجلسة (Session) داخل الواجهة                     |
| 6️⃣     | حماية الصفحات الخاصة (Dashboard)                      |
| 7️⃣     | ربط المستخدم بالحارس Guard                            |
| المفهوم           | الشرح                                 |
| ----------------- | ------------------------------------- |
| `User`            | هو الموديل الأساسي للمصادقة           |
| `@unique`         | يمنع تكرار البريد الإلكتروني          |
| `default(now())`  | يولّد تاريخ التسجيل تلقائيًا          |
| `prisma migrate`  | ينشئ الجداول فعليًا في قاعدة البيانات |
| `prisma generate` | يحدّث كائن prisma client في الكود     |
| `prisma studio`   | أداة رسومية لمشاهدة البيانات          |
npm install next-auth @auth/prisma-adapter bcryptjs zod
| الحزمة                 | الوظيفة                              |
| ---------------------- | ------------------------------------ |
| `next-auth`            | النظام الأساسي للمصادقة              |
| `@auth/prisma-adapter` | لربط NextAuth مع قاعدة بيانات Prisma |
| `bcryptjs`             | لتشفير كلمات المرور                  |
| `zod`                  | للتحقق من صحة البيانات (validation)  |
